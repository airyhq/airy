kubernetes.namespace=${KUBERNETES_NAMESPACE}
kubernetes.config=${KUBERNETES_CONFIG:}

#NOTE: For now will be fetching the installed components from api-components-installer until
#      we have the installed kafka topic
installer.components.list.url=${INSTALLER_COMPONENTS_LIST_URL:http://api-components-installer/components.list}
