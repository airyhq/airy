kafka.brokers=${KAFKA_BROKERS}
kafka.schema-registry-url=${KAFKA_SCHEMA_REGISTRY_URL}

kafka.suppress-interval-ms=${KAFKA_SUPPRESS_INTERVAL_MS:3000}

storage.s3.key=${STORAGE_S3_KEY}
storage.s3.secret=${STORAGE_S3_SECRET}
storage.s3.bucket=${STORAGE_S3_BUCKET}
storage.s3.region=${STORAGE_S3_REGION}
storage.s3.path=${STORAGE_S3_PATH:/}
