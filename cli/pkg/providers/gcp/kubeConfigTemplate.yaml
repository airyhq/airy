apiVersion: v1
clusters:
- cluster:
    server: {{.EndpointUrl}}
    certificate-authority-data: {{.CertificateData}}
  name: {{.ClusterName}}
contexts:
- context:
    cluster: {{.ClusterName}}
    user: {{.ClusterName}}
  name: {{.ClusterName}}
current-context: {{.ClusterName}}
kind: Config
preferences: {}
users:
- name: {{.ClusterName}}