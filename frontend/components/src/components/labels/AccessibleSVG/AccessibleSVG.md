A accessible enhancement of ReactSVG. Always use this instead of the
ReactSVG component.

It will always replace the title inside of the SVG. If no title is given it
will remove it.

```jsx
<AccessibleSVG src="src/assets/images/icons/trash.svg" title="This is the title"/>
```
