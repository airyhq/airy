apiVersion: apps/v1
kind: Deployment
metadata:
  name: sources-facebook-events-router
  namespace: default
  labels:
    app: sources-facebook-events-router
    boxImage: "no"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sources-facebook-events-router
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: sources-facebook-events-router
    spec:
      containers:
      - name: sources-facebook-events-router
        image: ghcr.io/airyhq/sources/facebook-events-router:latest
        imagePullPolicy: Always
        env:
        - name: KAFKA_BROKERS
          value: airy-cp-kafka-headless:9092
        - name: KAFKA_SCHEMA_REGISTRY_URL
          value: airy-kafka-cp-schema-registry:8081
        - name: FB_APP_ID
          value: "<fb_app_id>"
        - name: SERVICE_NAME
          value: facebook-events-router
---
apiVersion: v1
kind: Service
metadata:
  name: sources-facebook-events-router
  namespace: default
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    app: sources-facebook-events-router
