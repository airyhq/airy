apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: {{ .Values.global.namespace }}
data:
  SYSTEM_TOKEN: {{ .Values.systemToken | default (randAlphaNum 24) | quote }}
  ALLOWED_ORIGINS: {{ .Values.allowedOrigins | quote }}
