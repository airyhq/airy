apiVersion: v1
kind: ConfigMap
metadata:
  name: security
  namespace: {{ .Values.global.kubernetes.namespace }}
data:
{{- if .Values.systemToken }}
  systemToken: {{ .Values.systemToken | quote }}
{{- end }}
  allowedOrigins: {{ .Values.allowedOrigins | quote }}
