apiVersion: v1
kind: Service
metadata:
  name: frontend-demo
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: frontend-demo
