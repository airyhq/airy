apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
data:
  POSTGRES_DB: {{ .Values.dbName }}
  POSTGRES_USER: {{ .Values.pgUser }}
  POSTGRES_PASSWORD: {{ .Values.pgPassword }}