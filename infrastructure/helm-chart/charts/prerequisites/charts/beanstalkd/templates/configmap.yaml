apiVersion: v1
kind: ConfigMap
metadata:
  name: beanstalk-config
  namespace: {{ .Values.global.kubernetes.namespace }}
data:
  BEANSTALK_HOSTNAME: "beanstalk"
  BEANSTALK_PORT: "{{ .Values.port }}"
