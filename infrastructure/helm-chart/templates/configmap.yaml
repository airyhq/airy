apiVersion: v1
kind: ConfigMap
metadata:
  name: hostnames
  namespace: {{ .Values.global.namespace }}
data:
  {{ if .Values.global.ingress }}
  HOST: {{ .Values.global.ingress.host }}
  {{ else }}
  HOST: ""
  {{ end }}

